# 1. `git reset`
![三种模式，右侧表示保存或清空][git-reset.png]
1. 基本方式就是在指令后添加要回退的版本id，即提交时给到的代码
2. 如果没写参数，默认为mixed参数
# 2. `git reflog`
可以用于查找操作记录，但是感觉不如`git log --oneline`
# 3. `git diff`
* 用与查找工作区、暂存区、本地仓库的差异
* 什么也不加，查找工作区和暂存区的差异
* `HEAD` 工作区和版本库
* `cached` 暂存区和版本库
* `版本号 版本号`比较两个版本的差异
*  在上述参数后加文件名可以单看这个文件的差异
# HEAD
1. 表示当前分支的最新提交节点
2. `HEAD^`或`HEAD~`表示上一个版本
3. `HEAD~n`表示当前版本的上n个版本
# 4. `git rm`
![git-rm的参数说明][git-rm.png]

# 5.  .gitignore
![简单总结，ai太好用了][gitignore.png]
![][gitignore2.png]
# 6. 分支
这个可以在保证主功能可用下进行功能添加或者修复
1. `git baranch`
   查看当前仓库所有分支，带*表示目前所在分支
2. `git baranch 分支名`
   可以创建一个新的分支，但未更换到该分支
3. `git checkout 分支名`
   切换到某分支（默认），其后接文件名可以恢复某文件
   * `git switch`专门用于切换
   * `git checkout -b 分支名 提交id`可恢复某分支的状态，包括被删除的
1. `git merge 分支名`
   合并分支，分支名是将被合并的分支（非当前操作分支），被合并的分支不会消失，合并后会自动提交一次
2. `git branch -d 分支名`
   必须是被合并的分支才能用d，未合并用D
3. **如何处理合并冲突**
   当两个分支修改了同一个文件同一处时会产生冲突
	* `git merge --about`可终止合并
	* 手动修改文件
4. `git Rebase 分支名`
   分支名是目标分支（非当前操作分支），当前操作分支被接到目标分支的末尾
   merge是溪流合流，Rebase是截断断分支并接到目标分支
# 7. `git tag`
标记版本号用的